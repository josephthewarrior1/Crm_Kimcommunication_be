server.port=8080

spring.profiles.active=postgres

# # H2 in-memory database
# spring.datasource.url=jdbc:h2:mem:pmsdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=create
# spring.jpa.show-sql=true
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# # CORS dev convenience (adjust in production)
# app.cors.allowed-origins=http://localhost:3000, http://127.0.0.1:3000

# # File storage defaults
# storage.type=local
# storage.local-dir=uploads
# storage.s3.endpoint=http://localhost:9000
# storage.s3.accessKey=minioadmin
# storage.s3.secretKey=minioadmin
# storage.s3.bucket=pms-docs

# # Disable Liquibase on default (H2) profile; use Postgres profile for Liquibase
# spring.liquibase.enabled=false

# # Data seeding toggle (enabled by default for dev). Set to false in staging/prod.
# app.data.seed=true

# # Admin bootstrap user (dev/default)
# app.admin.username=admin
# app.admin.email=admin@example.com
# app.admin.password=admin
